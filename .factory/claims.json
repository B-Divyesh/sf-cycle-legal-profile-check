[
  {
    "id": "demo-sample-report",
    "claim": "Try it with sample data and ?demo=1 open a dated Brussels sample report immediately.",
    "where": "Landing primary action, /demo, ?demo=1, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sample-report",
    "sandbox": "Fresh browser context opens /demo and observes its report without setup."
  },
  {
    "id": "mapped-access-conflicts",
    "claim": "The sample identifies a vehicle-specific mapped access conflict in a planned GPX track.",
    "where": "Landing headline and description, sample report",
    "test": "npm run test:e2e -- --grep @claim:mapped-access-conflicts",
    "sandbox": "Fresh context uses the first-screen action and observes the speed-pedelec conflict and its mapped tag."
  },
  {
    "id": "demo-isolation",
    "claim": "The demo stays separate from real browser data and makes no analyzer or page-view request.",
    "where": "Demo banner, .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "Fresh context seeds a real license, opens /demo, resets it, then starts for real while recording API requests and storage keys."
  },
  {
    "id": "csv-export",
    "claim": "Exports the review checklist as CSV.",
    "where": "Report action, README",
    "test": "npm run test:e2e -- --grep @claim:csv-export",
    "sandbox": "Fresh context opens /demo, downloads the checklist, and asserts its header and both sample findings."
  },
  {
    "id": "offline-reload",
    "claim": "The app shell reloads offline after the first online visit.",
    "where": "Landing facts, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "Fresh browser context loads /demo, waits for the service worker, goes offline, then reloads the sample route."
  },
  {
    "id": "report-evidence",
    "claim": "Reports show mapped conflicts, uncertainty, OSM tags, and a dated rule source.",
    "where": "Report, How it works, README",
    "test": "npm run test:e2e -- --grep @claim:report-evidence",
    "sandbox": "Fresh context opens /demo and checks the visible evidence tag and expanded source date."
  },
  {
    "id": "vehicle-rule-profile",
    "claim": "A real check uses the uploaded GPX track, selected vehicle, and regional rule pack to return map evidence.",
    "where": "Checker, How it works, README",
    "test": "npm run test:e2e -- --grep @claim:vehicle-rule-profile",
    "sandbox": "Fresh context uploads a GPX fixture, selects speed pedelec and Belgium, inspects the request, and observes returned map evidence."
  },
  {
    "id": "regional-cycleway-decisions",
    "claim": "Belgium, Netherlands, and Germany use separate cautious speed-pedelec decisions for an untagged mapped cycleway.",
    "where": "README regional-rule-pack explanation",
    "test": "cargo test regional_cycleway_rules_are_distinct_and_cautious",
    "sandbox": "The analyzer receives the same highway=cycleway map tags for each regional pack and asserts distinct rule IDs, severities, and moped-path outcomes."
  },
  {
    "id": "gpx-size-limit",
    "claim": "GPX track uploads are limited to 8 MB.",
    "where": "GPX track file input",
    "test": "npm run test:e2e -- --grep @claim:gpx-size-limit",
    "sandbox": "Fresh context selects an 8 MB plus one byte fixture and observes rejection before any analyzer request."
  },
  {
    "id": "sampling-density",
    "claim": "The checker samples at 80 metres or one-sixtieth of route length, whichever is farther apart.",
    "where": "README Data and limits",
    "test": "cargo test sampling_rule_uses_eighty_metres_or_one_sixtieth_of_route_length",
    "sandbox": "Synthetic point distances exercise the 80-metre floor and a six-kilometre route with 60 equal sampling intervals."
  },
  {
    "id": "matching-radius",
    "claim": "The checker searches for highway geometry within 35 metres.",
    "where": "README Data and limits, report caveats",
    "test": "cargo test matching_radius_is_thirty_five_metres",
    "sandbox": "A 34-metre segment is included, a 36-metre segment is excluded, and the generated Overpass query is asserted to use 35 metres."
  },
  {
    "id": "regional-pricing",
    "claim": "Belgium checks and checklist export are free; €19 once adds the Netherlands and Germany regional rule packs.",
    "where": "Landing facts, pricing section, README, terms",
    "test": "npm run test:e2e -- --grep @claim:regional-pricing",
    "sandbox": "Fresh context exports the demo report without a license, observes the one-time price, then uses a recorded valid verdict to enable both paid rule packs."
  },
  {
    "id": "billing-refunds",
    "claim": "Sociobot billing, backed by Dodo, handles checkout and refunds; a refunded or revoked license removes paid regional rule packs.",
    "where": "Pricing section, terms",
    "test": "npm run test:e2e -- --grep @claim:billing-refunds",
    "sandbox": "Fresh context checks the Sociobot checkout URL and terms, then recorded valid and revoked verdicts prove paid options are added and removed."
  },
  {
    "id": "license-browser-local",
    "claim": "A returned license stays in the browser, is verified through Sociobot, and is not reverified for one day.",
    "where": "Privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:license-browser-local",
    "sandbox": "Fresh context enters /demo, starts for real, receives a mocked license return, and verifies storage, URL cleanup, and once-daily caching."
  },
  {
    "id": "gpx-not-retained",
    "claim": "The server does not retain GPX track data in SQLite.",
    "where": "Privacy page, README",
    "test": "cargo test gpx_analysis_never_persists_route_data",
    "sandbox": "A temporary SQLite database analyzes a named GPX and asserts that only the aggregate-counter table exists with no rows."
  },
  {
    "id": "aggregate-page-view",
    "claim": "The server stores only one aggregate page-view counter.",
    "where": "Privacy page, README",
    "test": "cargo test page_views_persist_only_an_aggregate_counter",
    "sandbox": "A temporary SQLite database receives two page-view calls and asserts its sole row is the aggregate counter."
  },
  {
    "id": "ip-not-persisted",
    "claim": "Client IP addresses used for request limits are not written to SQLite.",
    "where": "Privacy page",
    "test": "cargo test client_ip_addresses_are_not_persisted",
    "sandbox": "A temporary app receives a page-view request with a fixed forwarded client IP, then its complete SQLite schema and rows are inspected."
  },
  {
    "id": "overpass-data-disclosure",
    "claim": "A check sends sampled coordinates to Overpass without sending the GPX file or track name.",
    "where": "Privacy page",
    "test": "cargo test overpass_receives_sampled_coordinates_without_the_gpx_file_or_track_name",
    "sandbox": "A local Overpass spy captures the form body and asserts the sampled coordinates and 35-metre queries are present while the raw GPX and private track name are absent."
  },
  {
    "id": "browser-storage-removal",
    "claim": "Remove saved browser data deletes the saved license, cached verdict, and demo marker.",
    "where": "Privacy page",
    "test": "npm run test:e2e -- --grep @claim:browser-storage-removal",
    "sandbox": "A fresh browser context seeds every product storage key, uses the privacy-page removal control, and asserts every key is gone."
  },
  {
    "id": "api-rate-limit",
    "claim": "Both API routes allow a burst of 40 requests per client and replenish at 20 requests per second.",
    "where": "README Verify",
    "test": "cargo test api_rate_limit_allows_forty_then_replenishes_at_twenty_per_second",
    "sandbox": "A fixed forwarded client address receives 40 page-view responses, a 429 with Retry-After, then one renewed allowance after the 50-millisecond refill interval."
  }
]
